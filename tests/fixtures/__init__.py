"""
Test fixtures for PHASE 2 edge case testing.

This package provides:
- Authentication fixtures (valid/expired tokens, users, roles)
- Data fixtures (empty, null, large datasets)
- Service mocks (DB unavailable, slow APIs, network failures)
- Test data factories (create test objects quickly)
"""
